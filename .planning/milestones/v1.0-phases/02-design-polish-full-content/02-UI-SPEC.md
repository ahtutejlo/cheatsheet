---
phase: 2
slug: design-polish-full-content
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-20
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Design Polish and Full Content. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (Astro components, no framework) |
| Icon library | Inline SVG (sun/moon for theme toggle, chevron/link icons already in use) |
| Font | System font stack (Tailwind default: ui-sans-serif, system-ui, sans-serif) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, icon-to-text gaps |
| md | 16px | Default element padding (p-4), card internal spacing |
| lg | 24px | Section card padding (p-6), page vertical rhythm |
| xl | 32px | Layout gaps between section groups |
| 2xl | 48px | Major section breaks, page top/bottom padding |
| 3xl | 64px | Not used in this phase |

Exceptions: none. Touch target minimum (44px on theme toggle and flashcard summary) is inherited from Phase 1 via `min-h-[44px]` and is not a spacing token in this phase.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (text-base) | 400 (font-normal) | 1.5 (leading-relaxed) |
| Label | 14px (text-sm) | 400 (font-normal) | 1.5 |
| Heading | 24px (text-2xl) | 700 (font-bold) | 1.2 |
| Display | 36px (text-4xl) | 700 (font-bold) | 1.2 |

Notes:
- No responsive font size scaling. All 4 sizes are fixed across all breakpoints.
- Distinct pixel values: 14, 16, 24, 36 -- exactly 4.
- Question text in flashcard summary: 16px (text-base) at weight 400 at all breakpoints. No size or weight increase on sm+.
- Code blocks inside prose: 14px (text-sm) via `prose-pre:text-sm` (already set in Phase 1).
- No custom fonts. System stack keeps load time zero and avoids FOUT.
- Only 2 weights used: 400 (normal) and 700 (bold). No font-medium (500).

---

## Color

### Light Mode (default)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #ffffff (white) | Page background, card backgrounds |
| Secondary (30%) | #f9fafb (gray-50) | Flashcard hover bg, code block bg, subtle surfaces |
| Accent (10%) | #2563eb (blue-600) | See reserved-for list below |
| Destructive | not applicable | No destructive actions in this phase |

### Dark Mode

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #111827 (gray-900) | Page background |
| Secondary (30%) | #1f2937 (gray-800) | Card backgrounds, elevated surfaces |
| Accent (10%) | #3b82f6 (blue-500) | See reserved-for list below |
| Destructive | not applicable | No destructive actions in this phase |

### Border Colors

| Mode | Value | Usage |
|------|-------|-------|
| Light | #e5e7eb (gray-200) | Card borders, dividers, header bottom border |
| Dark | #374151 (gray-700) | Card borders, dividers, header bottom border |

### Text Colors

| Role | Light | Dark |
|------|-------|------|
| Primary text | #111827 (gray-900) | #f3f4f6 (gray-100) |
| Secondary text | #4b5563 (gray-600) | #9ca3af (gray-400) |
| Muted text | #9ca3af (gray-400) | #6b7280 (gray-500) |

### Accent Reserved For

Accent color is reserved for these elements only -- never used for general backgrounds or body text:

1. Section card question count badge text (`text-blue-600` / `dark:text-blue-400`)
2. Section card heading hover state (`group-hover:text-blue-600` / `dark:group-hover:text-blue-400`)
3. Header site title hover state
4. Flashcard anchor link hover state (`hover:text-blue-500`)
5. Flashcard highlight ring on anchor navigation (`box-shadow: 0 0 0 2px #3b82f6`)
6. Section card left accent strip (new in Phase 2: 3px left border on hover)
7. Theme toggle button hover background uses gray, not accent

---

## Visual Hierarchy

Primary focal point: section card grid -- hero subtitle and section cards are the first visual anchors; all other elements are secondary.

---

## Dark Mode Contract

| Property | Value |
|----------|-------|
| Toggle mechanism | Class-based (`html.dark`) via `@custom-variant dark` in Tailwind v4 CSS |
| Persistence | localStorage key `theme` with values `dark` or `light` |
| System fallback | `window.matchMedia('(prefers-color-scheme: dark)')` when no localStorage value |
| FOUC prevention | Inline `<script is:inline>` in `<head>` before body renders |
| Toggle placement | Header, right side, single button with sun/moon icon swap |
| Prose inversion | `dark:prose-invert` on flashcard answer container |
| Shiki code blocks | Remove `@media (prefers-color-scheme: dark)` rules; keep only `html.dark` selectors |

---

## Animation and Transition Contract

| Element | Property | Duration | Easing | Trigger |
|---------|----------|----------|--------|---------|
| All interactive color changes | color, background-color, border-color | 200ms | ease | hover, focus |
| Flashcard answer reveal | opacity, transform(translateY) | 200ms | ease-out | details[open] |
| Flashcard chevron rotation | transform(rotate) | 200ms | default | details[open] |
| Section card hover shadow | box-shadow | 200ms | ease | hover |
| Section card left accent strip | border-left-color | 200ms | ease | hover |
| Theme toggle icon swap | instant (hidden class toggle) | 0ms | none | click |

No new animation libraries. All transitions use Tailwind `transition-*` utilities or the existing `@keyframes flashcard-reveal`.

---

## Copywriting Contract

| Element | Copy (UA) | Copy (EN) |
|---------|-----------|-----------|
| Primary CTA (home page subtitle) | Оберіть тему для підготовки | Choose a topic to study |
| Section back link | Назад до розділів | Back to sections |
| Theme toggle aria-label | Перемкнути тему | Toggle dark mode |
| Empty state heading | Питань поки немає | No questions yet |
| Empty state body | Цей розділ ще наповнюється. Поверніться пізніше. | This section is being populated. Check back soon. |
| Error state | not applicable (static site, no runtime errors in this phase) | not applicable |
| Destructive confirmation | not applicable (no destructive actions) | not applicable |

New i18n keys needed for Phase 2:

| Key | UA | EN |
|-----|----|----|
| `theme.toggle` | Перемкнути тему | Toggle dark mode |
| `section.empty.title` | Питань поки немає | No questions yet |
| `section.empty.body` | Цей розділ ще наповнюється. Поверніться пізніше. | This section is being populated. Check back soon. |

---

## Component Dark Mode Mapping

Every existing component must receive `dark:` variants. Specific mapping:

### BaseLayout.astro
| Current | Add |
|---------|-----|
| `bg-white text-gray-900` | `dark:bg-gray-900 dark:text-gray-100` |

### Header.astro
| Current | Add |
|---------|-----|
| `border-gray-200 bg-white` | `dark:border-gray-700 dark:bg-gray-900` |
| `text-gray-900` | `dark:text-gray-100` |
| `hover:text-blue-600` | `dark:hover:text-blue-400` |

### SectionCard.astro
| Current | Add |
|---------|-----|
| `border-gray-200` | `dark:border-gray-700` |
| `hover:border-blue-300` | `dark:hover:border-blue-600` |
| `text-gray-900` | `dark:text-gray-100` |
| `text-gray-600` | `dark:text-gray-400` |
| `text-blue-600` | `dark:text-blue-400` |
| `group-hover:text-blue-600` | `dark:group-hover:text-blue-400` |

### Flashcard.astro
| Current | Add |
|---------|-----|
| `border-gray-200` | `dark:border-gray-700` |
| `text-gray-900` | `dark:text-gray-100` |
| `hover:bg-gray-50` | `dark:hover:bg-gray-800` |
| `text-gray-300` (anchor icon) | `dark:text-gray-600` |
| `hover:text-blue-500` | `dark:hover:text-blue-400` |
| `text-gray-400` (chevron) | `dark:text-gray-500` |
| `prose-pre:bg-gray-100` | `dark:prose-pre:bg-gray-800` |
| `prose-pre:border-gray-200` | `dark:prose-pre:border-gray-700` |
| Add `dark:prose-invert` to prose container | |

### Section page (index.astro)
| Current | Add |
|---------|-----|
| `text-gray-900` on headings | `dark:text-gray-100` |
| Any `text-gray-600` on subtitles | `dark:text-gray-400` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No component registry in use. All components are hand-authored Astro components with Tailwind CSS utilities. No registry vetting required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
