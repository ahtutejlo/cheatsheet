---
phase: 05-content-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/questions/java/jmm-happens-before.md
  - src/content/questions/java/concurrent-hashmap-internals.md
  - src/content/questions/java/class-loading-mechanism.md
  - src/content/questions/java/jit-compilation.md
  - src/content/questions/java/string-interning-details.md
  - src/content/questions/java/integer-caching-trap.md
  - src/content/questions/java/autoboxing-equals-trap.md
  - src/content/questions/java/finally-return-trap.md
  - src/content/questions/java/checked-unchecked-exceptions-trap.md
  - src/content/questions/java/immutable-string-trap.md
  - src/content/questions/java/debug-memory-leak-scenario.md
  - src/content/questions/java/thread-safe-cache-scenario.md
  - src/content/questions/java/optimize-gc-latency-scenario.md
  - src/content/questions/java/plugin-system-classloaders-scenario.md
  - src/content/questions/java/distributed-transactions-scenario.md
autonomous: true
requirements: [CONT-04, CONT-05, CONT-06]

must_haves:
  truths:
    - "Java section has 30 questions total (15 basic + 15 new)"
    - "5 deep questions cover JMM, ConcurrentHashMap, class loading, JIT, String interning"
    - "5 trick questions each start with > **Trap:** blockquote"
    - "5 practical questions each use Scenario/Approach/Solution structure"
    - "All questions are bilingual UA/EN with identical code blocks"
  artifacts:
    - path: "src/content/questions/java/jmm-happens-before.md"
      provides: "Deep question order 16"
      contains: "type: \"deep\""
    - path: "src/content/questions/java/integer-caching-trap.md"
      provides: "Trick question order 21"
      contains: "Trap:"
    - path: "src/content/questions/java/debug-memory-leak-scenario.md"
      provides: "Practical question order 26"
      contains: "**Scenario:**"
  key_links:
    - from: "src/content/questions/java/*.md"
      to: "src/content.config.ts"
      via: "Zod schema validation at build time"
      pattern: "section: \"java\""
---

<objective>
Create 15 advanced bilingual questions for the Java section: 5 deep-technical, 5 trick, and 5 practical.

Purpose: Expand Java from 15 basic questions to 30 total, covering JVM internals, common Java traps, and production debugging scenarios.
Output: 15 new Markdown files in src/content/questions/java/
</objective>

<execution_context>
@/Users/ahtutejlo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahtutejlo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CONTENT_CONVENTIONS.md
@.planning/phases/05-content-generation/05-RESEARCH.md
@.planning/phases/05-content-generation/05-UI-SPEC.md

<interfaces>
z.object({
  ua_question: z.string(),
  en_question: z.string(),
  ua_answer: z.string(),
  en_answer: z.string(),
  section: z.string(),
  order: z.number(),
  tags: z.array(z.string()).default([]),
  type: z.enum(['basic', 'deep', 'trick', 'practical']).default('basic'),
})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 15 Java advanced question files</name>
  <files>
    src/content/questions/java/jmm-happens-before.md
    src/content/questions/java/concurrent-hashmap-internals.md
    src/content/questions/java/class-loading-mechanism.md
    src/content/questions/java/jit-compilation.md
    src/content/questions/java/string-interning-details.md
    src/content/questions/java/integer-caching-trap.md
    src/content/questions/java/autoboxing-equals-trap.md
    src/content/questions/java/finally-return-trap.md
    src/content/questions/java/checked-unchecked-exceptions-trap.md
    src/content/questions/java/immutable-string-trap.md
    src/content/questions/java/debug-memory-leak-scenario.md
    src/content/questions/java/thread-safe-cache-scenario.md
    src/content/questions/java/optimize-gc-latency-scenario.md
    src/content/questions/java/plugin-system-classloaders-scenario.md
    src/content/questions/java/distributed-transactions-scenario.md
  </files>
  <read_first>
    CONTENT_CONVENTIONS.md
    src/content.config.ts
    src/content/questions/java/jvm-architecture.md
    src/content/questions/java/multithreading-basics.md
    src/content/questions/java/string-pool.md
    .planning/phases/05-content-generation/05-RESEARCH.md
  </read_first>
  <action>
Create 15 Markdown files in src/content/questions/java/. Each file is YAML-frontmatter-only (no body content after the closing ---).

**All files must have:**
- `ua_question` and `en_question` wrapped in double quotes
- `ua_answer` and `en_answer` using `|` block scalar with 2-space indent
- `section: "java"`
- `tags:` array with 2-4 lowercase hyphenated tags
- `type:` explicitly set (never omit)
- Code blocks with `java` language tag
- Code blocks IDENTICAL in UA and EN versions
- Ukrainian text uses natural phrasing, technical terms stay English

**DEEP questions (order 16-20, type: "deep"):**
1. `jmm-happens-before.md` (order 16) — Java Memory Model, happens-before guarantees, volatile/synchronized ordering. Show code with volatile flag pattern. Tags: [concurrency, jvm, memory-model]
2. `concurrent-hashmap-internals.md` (order 17) — ConcurrentHashMap segment locking (Java 7) vs node-level CAS (Java 8+), how concurrent reads work without locking. Tags: [concurrency, collections, internals]
3. `class-loading-mechanism.md` (order 18) — Three-tier class loading (Bootstrap/Extension/Application), parent delegation model, when custom classloaders are needed. Tags: [jvm, classloading, architecture]
4. `jit-compilation.md` (order 19) — JIT compiler tiers (C1/C2), method inlining, escape analysis, on-stack replacement. Show JVM flags for inspecting compilation. Tags: [jvm, performance, compilation]
5. `string-interning-details.md` (order 20) — String pool internals, intern() behavior, memory implications, -XX:StringTableSize tuning. Tags: [strings, jvm, memory]

**TRICK questions (order 21-25, type: "trick"):**
Each answer MUST start with `> **Trap:** ...` blockquote:
1. `integer-caching-trap.md` (order 21) — Trap: == always works for Integer comparison. Reality: Integer caches -128 to 127 only; beyond that == compares references. Show `new Integer(127) == new Integer(127)` vs `new Integer(128) == new Integer(128)`. Tags: [autoboxing, pitfalls, comparisons]
2. `autoboxing-equals-trap.md` (order 22) — Trap: equals() and == are interchangeable for wrapper types. Reality: == on boxed types compares references except in cached range; always use equals(). Tags: [autoboxing, equals, pitfalls]
3. `finally-return-trap.md` (order 23) — Trap: return in try block skips finally. Reality: finally ALWAYS executes, and return in finally overrides return in try. Show code demonstrating override. Tags: [exceptions, control-flow, pitfalls]
4. `checked-unchecked-exceptions-trap.md` (order 24) — Trap: RuntimeException is checked because it extends Exception. Reality: RuntimeException and its subclasses are unchecked; only non-Runtime Exception subclasses are checked. Tags: [exceptions, error-handling, pitfalls]
5. `immutable-string-trap.md` (order 25) — Trap: String concatenation in a loop is efficient because strings are objects. Reality: each += creates a new String object; use StringBuilder for loops. Show bytecode/performance difference. Tags: [strings, performance, pitfalls]

**PRACTICAL questions (order 26-30, type: "practical"):**
Each answer MUST use **Scenario:**/**Approach:**/**Solution:** structure:
1. `debug-memory-leak-scenario.md` (order 26) — Scenario: production app OOM after 48 hours. Approach: heap dump, MAT analysis, identify retained objects. Solution: code fix for unclosed resources or growing cache. Tags: [debugging, memory, jvm]
2. `thread-safe-cache-scenario.md` (order 27) — Scenario: implement in-memory cache for frequently accessed DB records, used by 50 concurrent threads. Approach: ConcurrentHashMap vs synchronized, eviction policy, atomic operations. Tags: [concurrency, caching, design]
3. `optimize-gc-latency-scenario.md` (order 28) — Scenario: trading app needs sub-10ms GC pauses. Approach: GC log analysis, collector selection (ZGC/Shenandoah), heap sizing. Solution: JVM flags configuration. Tags: [gc, performance, jvm]
4. `plugin-system-classloaders-scenario.md` (order 29) — Scenario: build plugin architecture where plugins can be loaded/unloaded at runtime without restarting. Approach: custom ClassLoader per plugin, interface contracts, isolation. Tags: [classloading, architecture, design]
5. `distributed-transactions-scenario.md` (order 30) — Scenario: order service must coordinate payment and inventory across microservices. Approach: saga pattern vs 2PC, compensating transactions. Solution: saga orchestrator implementation. Tags: [microservices, transactions, patterns]

Each answer should be 3-6 paragraphs total with substantive technical content.
  </action>
  <verify>
    <automated>cd /Users/ahtutejlo/projects/cheatsheet && ls src/content/questions/java/*.md | wc -l | tr -d ' ' && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ls src/content/questions/java/*.md | wc -l returns 30
    - pnpm build exits 0 with no YAML parse errors
    - grep -c 'type: "deep"' src/content/questions/java/jmm-happens-before.md returns 1
    - grep -c 'Trap:' src/content/questions/java/integer-caching-trap.md returns at least 2 (UA + EN)
    - grep -c '**Scenario:**' src/content/questions/java/debug-memory-leak-scenario.md returns at least 2 (UA + EN)
    - grep -c 'section: "java"' src/content/questions/java/jmm-happens-before.md returns 1
    - All deep files have order 16-20, trick files 21-25, practical files 26-30
  </acceptance_criteria>
  <done>15 new Java question files exist with correct frontmatter, build succeeds, section total is 30 files</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with 0 errors
- `ls src/content/questions/java/*.md | wc -l` returns 30
- All trick files contain `> **Trap:**` pattern
- All practical files contain `**Scenario:**`, `**Approach:**`, `**Solution:**`
- All files have `section: "java"`
</verification>

<success_criteria>
Java section has 30 bilingual questions (15 basic + 5 deep + 5 trick + 5 practical), all passing Astro build validation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-generation/05-02-SUMMARY.md`
</output>
